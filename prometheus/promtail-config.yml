server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: https://logs-prod-020.grafana.net/loki/api/v1/push
    basic_auth:
      username: '1667994'
      password: 'glc_eyJvIjoiMTgzMTMyMSIsIm4iOiJ0ZXN0LXRlc3QiLCJrIjoieTBkODIyVDc3S2VUU3k3bzdzOFVFZjJJIiwibSI6eyJyIjoidXMifX0='

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
      - source_labels: ['__meta_docker_container_log_stream']
        target_label: 'stream'
