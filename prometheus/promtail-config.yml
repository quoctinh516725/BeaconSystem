server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: https://logs-prod-020.grafana.net/loki/api/v1/push
    basic_auth:
      username: '1667994'
      password: 'glc_eyJvIjoiMTgzMTMyMSIsIm4iOiJzdGFjay0xNzEwMjA3LWhsLXdyaXRlLXRva2VuIiwiayI6Ing5MDZSeERKNU5vUzBaekVENDJNODAwbSIsIm0iOnsiciI6InByb2QtYXAtc291dGhlYXN0LTEifX0='

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    pipeline_stages:
      - drop:
          expression: '.*GET /metrics.*'
    relabel_configs:
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
      - source_labels: ['__meta_docker_container_log_stream']
        target_label: 'stream'
